{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build:web": "npm --prefix ../web run build",
    "typecheck": "tsc -b --noEmit",
    "build": "tsc -b",
    "start": "node dist/index.js",
    "start:http": "MCP_HTTP=true node dist/index.js",
    "deploy:install": "npm install && cd ../web && npm install && npm run build && cd ../server && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "express": "^5.1.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
