{
  "name": "web",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:detail": "npx esbuild src/pokemon-detail.tsx --bundle --format=esm --outfile=dist/pokemon-detail.js",
    "detail": "npm run build:detail -- --servedir=dist --serve=8001 --watch",
    "build": "npx esbuild src/component.tsx src/pokemon-detail.tsx --bundle --format=esm --outdir=dist && cp index.html dist/index.html",
    "start": "npx esbuild src/component.tsx --bundle --format=esm --outfile=dist/component.js --servedir=dist --serve=8000 --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.2",
    "@types/react-dom": "^19.2.2",
    "esbuild": "^0.25.12",
    "typescript": "^5.9.3"
  }
}
